//import fs from 'fs-extra';

export const writeJsonSync = (filename, json) => {
  //const dir = 'C:/xampp/htdocs/SpaceHuddle/frontend/src/locales';
  //fse.ensureDirSync(dir);
  //fse.writeJsonSync(filename, json)
};